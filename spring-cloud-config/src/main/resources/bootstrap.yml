server:
  port: 8888
spring:
  application:
    name: zuul
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LHRH/spring-cloud-config-files
          search-paths: respo
      label: master